document.addEventListener('DOMContentLoaded', () => {
  console.log('JavaScript loaded');

  const newItemForm = document.querySelector('form#form-new-item');
  newItemForm.addEventListener('submit', handleNewFormSubmit);



})

  const handleNewFormSubmit = function (formSubmitEvent){
    event.preventDefault();

    


  }